---
---

<header class="fixed top-0 left-0 right-0 z-50 bg-white/95 backdrop-blur-sm shadow-md">
	<nav class="container mx-auto px-4 py-4">
		<div class="flex items-center justify-between">
			<a href="/" class="flex items-center gap-2">
				<div class="w-12 h-12 bg-gradient-to-br from-primary-500 to-secondary-500 rounded-full flex items-center justify-center">
					<span class="text-2xl">🎈</span>
				</div>
				<span class="text-3xl font-bold bg-gradient-to-r from-primary-600 to-secondary-600 bg-clip-text text-transparent">
					Anima't
				</span>
			</a>

			<button
				id="mobile-menu-btn"
				class="lg:hidden p-2 text-gray-600 hover:text-primary-600 transition-colors"
				aria-label="Toggle menu"
			>
				<svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
					<path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
				</svg>
			</button>

			<ul class="hidden lg:flex items-center gap-8">
				<li><a href="#inicio" class="text-gray-700 hover:text-primary-600 font-medium transition-colors">Inicio</a></li>
				<li><a href="#servicios" class="text-gray-700 hover:text-primary-600 font-medium transition-colors">Servicios</a></li>
				<li><a href="#contacto" class="text-gray-700 hover:text-primary-600 font-medium transition-colors">Contacto</a></li>
				<li>
					<a
						href="#reservar"
						class="px-6 py-3 bg-gradient-to-r from-primary-500 to-secondary-500 text-white font-semibold rounded-full hover:shadow-lg hover:scale-105 transition-all duration-300"
					>
						¡Reserva Ahora!
					</a>
				</li>
			</ul>
		</div>

		<div id="mobile-menu" class="hidden lg:hidden mt-4 pb-4 border-t border-gray-200 pt-4">
			<ul class="flex flex-col gap-4">
				<li><a href="#inicio" class="block text-gray-700 hover:text-primary-600 font-medium transition-colors">Inicio</a></li>
				<li><a href="#servicios" class="block text-gray-700 hover:text-primary-600 font-medium transition-colors">Servicios</a></li>
				<li><a href="#contacto" class="block text-gray-700 hover:text-primary-600 font-medium transition-colors">Contacto</a></li>
				<li>
					<a
						href="#reservar"
						class="block text-center px-6 py-3 bg-gradient-to-r from-primary-500 to-secondary-500 text-white font-semibold rounded-full hover:shadow-lg transition-all duration-300"
					>
						¡Reserva Ahora!
					</a>
				</li>
			</ul>
		</div>
	</nav>
</header>

<script>
	const mobileMenuBtn = document.getElementById('mobile-menu-btn');
	const mobileMenu = document.getElementById('mobile-menu');

	mobileMenuBtn?.addEventListener('click', () => {
		mobileMenu?.classList.toggle('hidden');
	});

	document.querySelectorAll('#mobile-menu a').forEach((link) => {
		link.addEventListener('click', () => {
			mobileMenu?.classList.add('hidden');
		});
	});
</script>
